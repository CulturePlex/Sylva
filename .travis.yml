language: python
python:
  - "2.7"
install:
  - pip install -U -r requirements.txt --use-mirrors
#  - pip install coveralls --use-mirrors
script:
  - python sylva/manage.py test accounts
after_success:
#  - coveralls
